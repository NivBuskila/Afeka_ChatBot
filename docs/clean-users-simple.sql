-- סקריפט פשוט לניקוי טבלאות משתמשים ומנהלים בלבד

-- ניקוי טבלת המנהלים
DELETE FROM admins;

-- ניקוי טבלת המשתמשים
DELETE FROM users;

-- איפוס ערכי SERIAL (אם ישנם)
-- זה יגרום למזהים להתחיל מחדש מ-1
ALTER SEQUENCE admins_id_seq RESTART WITH 1;

-- הערה: סקריפט זה מסיר רק את הנתונים מהטבלאות הספציפיות
-- הוא לא מנקה את טבלת auth.users, לכן ייתכן שתצטרך לטפל בכך בנפרד
-- או להשתמש בסקריפט המלא יותר 